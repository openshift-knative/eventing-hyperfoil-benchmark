# Specify some production-like arbitrary resource specs.

spec:
  template:
    spec:
      containers:
        - name: kafka-broker-receiver
          resources:
            requests:
              memory: "1Gi"
              cpu: "1"
