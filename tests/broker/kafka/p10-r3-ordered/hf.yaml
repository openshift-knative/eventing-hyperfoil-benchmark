
name: eventing-staircase
agents:
  agent-one:
    node: ${WORKER_ONE}
    stop: true
  agent-two:
    node: ${WORKER_ONE}
    stop: true
  agent-three:
    node: ${WORKER_ONE}
    stop: true
http:
  host: ${HTTP_HOST}
  sharedConnections: 10000
staircase:
  initialRampUpDuration: 60s
  initialUsersPerSec: 500
  incrementUsersPerSec: 100
  steadyStateDuration: 120s
  maxIterations: 20
  maxSessions: 10000
  rampUpDuration: 120s
  scenario:
  
    - broker-ord-0:
        - timestamp:
            toVar: eventTimestamp
        - httpRequest:
            POST: /${TEST_CASE_NAMESPACE}/broker-ord-0
            body: |
              {"foo" : "bar"}
            headers:
              ce-benchmarktimestamp: "${eventTimestamp}"
              ce-id: abc
              ce-metric: e2elatency
              ce-phase: "${hyperfoil.phase.id}"
              ce-phasestart: "${hyperfoil.phase.start.time.as.string}"
              ce-runid: "${hyperfoil.run.id}"
              ce-source: "http://hyperfoil-bench.com"
              ce-specversion: "1.0"
              ce-type: datapoint.hyperfoilbench
              content-type: application/json
            sla:
              - meanResponseTime: 1s
              - limits:
                  "0.999": 1s


    - broker-ord-1:
        - timestamp:
            toVar: eventTimestamp
        - httpRequest:
            POST: /${TEST_CASE_NAMESPACE}/broker-ord-1
            body: |
              {"foo" : "bar"}
            headers:
              ce-benchmarktimestamp: "${eventTimestamp}"
              ce-id: abc
              ce-metric: e2elatency
              ce-phase: "${hyperfoil.phase.id}"
              ce-phasestart: "${hyperfoil.phase.start.time.as.string}"
              ce-runid: "${hyperfoil.run.id}"
              ce-source: "http://hyperfoil-bench.com"
              ce-specversion: "1.0"
              ce-type: datapoint.hyperfoilbench
              content-type: application/json
            sla:
              - meanResponseTime: 1s
              - limits:
                  "0.999": 1s


    - broker-ord-2:
        - timestamp:
            toVar: eventTimestamp
        - httpRequest:
            POST: /${TEST_CASE_NAMESPACE}/broker-ord-2
            body: |
              {"foo" : "bar"}
            headers:
              ce-benchmarktimestamp: "${eventTimestamp}"
              ce-id: abc
              ce-metric: e2elatency
              ce-phase: "${hyperfoil.phase.id}"
              ce-phasestart: "${hyperfoil.phase.start.time.as.string}"
              ce-runid: "${hyperfoil.run.id}"
              ce-source: "http://hyperfoil-bench.com"
              ce-specversion: "1.0"
              ce-type: datapoint.hyperfoilbench
              content-type: application/json
            sla:
              - meanResponseTime: 1s
              - limits:
                  "0.999": 1s


    - broker-ord-3:
        - timestamp:
            toVar: eventTimestamp
        - httpRequest:
            POST: /${TEST_CASE_NAMESPACE}/broker-ord-3
            body: |
              {"foo" : "bar"}
            headers:
              ce-benchmarktimestamp: "${eventTimestamp}"
              ce-id: abc
              ce-metric: e2elatency
              ce-phase: "${hyperfoil.phase.id}"
              ce-phasestart: "${hyperfoil.phase.start.time.as.string}"
              ce-runid: "${hyperfoil.run.id}"
              ce-source: "http://hyperfoil-bench.com"
              ce-specversion: "1.0"
              ce-type: datapoint.hyperfoilbench
              content-type: application/json
            sla:
              - meanResponseTime: 1s
              - limits:
                  "0.999": 1s


    - broker-ord-4:
        - timestamp:
            toVar: eventTimestamp
        - httpRequest:
            POST: /${TEST_CASE_NAMESPACE}/broker-ord-4
            body: |
              {"foo" : "bar"}
            headers:
              ce-benchmarktimestamp: "${eventTimestamp}"
              ce-id: abc
              ce-metric: e2elatency
              ce-phase: "${hyperfoil.phase.id}"
              ce-phasestart: "${hyperfoil.phase.start.time.as.string}"
              ce-runid: "${hyperfoil.run.id}"
              ce-source: "http://hyperfoil-bench.com"
              ce-specversion: "1.0"
              ce-type: datapoint.hyperfoilbench
              content-type: application/json
            sla:
              - meanResponseTime: 1s
              - limits:
                  "0.999": 1s


    - broker-ord-5:
        - timestamp:
            toVar: eventTimestamp
        - httpRequest:
            POST: /${TEST_CASE_NAMESPACE}/broker-ord-5
            body: |
              {"foo" : "bar"}
            headers:
              ce-benchmarktimestamp: "${eventTimestamp}"
              ce-id: abc
              ce-metric: e2elatency
              ce-phase: "${hyperfoil.phase.id}"
              ce-phasestart: "${hyperfoil.phase.start.time.as.string}"
              ce-runid: "${hyperfoil.run.id}"
              ce-source: "http://hyperfoil-bench.com"
              ce-specversion: "1.0"
              ce-type: datapoint.hyperfoilbench
              content-type: application/json
            sla:
              - meanResponseTime: 1s
              - limits:
                  "0.999": 1s


    - broker-ord-6:
        - timestamp:
            toVar: eventTimestamp
        - httpRequest:
            POST: /${TEST_CASE_NAMESPACE}/broker-ord-6
            body: |
              {"foo" : "bar"}
            headers:
              ce-benchmarktimestamp: "${eventTimestamp}"
              ce-id: abc
              ce-metric: e2elatency
              ce-phase: "${hyperfoil.phase.id}"
              ce-phasestart: "${hyperfoil.phase.start.time.as.string}"
              ce-runid: "${hyperfoil.run.id}"
              ce-source: "http://hyperfoil-bench.com"
              ce-specversion: "1.0"
              ce-type: datapoint.hyperfoilbench
              content-type: application/json
            sla:
              - meanResponseTime: 1s
              - limits:
                  "0.999": 1s


    - broker-ord-7:
        - timestamp:
            toVar: eventTimestamp
        - httpRequest:
            POST: /${TEST_CASE_NAMESPACE}/broker-ord-7
            body: |
              {"foo" : "bar"}
            headers:
              ce-benchmarktimestamp: "${eventTimestamp}"
              ce-id: abc
              ce-metric: e2elatency
              ce-phase: "${hyperfoil.phase.id}"
              ce-phasestart: "${hyperfoil.phase.start.time.as.string}"
              ce-runid: "${hyperfoil.run.id}"
              ce-source: "http://hyperfoil-bench.com"
              ce-specversion: "1.0"
              ce-type: datapoint.hyperfoilbench
              content-type: application/json
            sla:
              - meanResponseTime: 1s
              - limits:
                  "0.999": 1s


    - broker-ord-8:
        - timestamp:
            toVar: eventTimestamp
        - httpRequest:
            POST: /${TEST_CASE_NAMESPACE}/broker-ord-8
            body: |
              {"foo" : "bar"}
            headers:
              ce-benchmarktimestamp: "${eventTimestamp}"
              ce-id: abc
              ce-metric: e2elatency
              ce-phase: "${hyperfoil.phase.id}"
              ce-phasestart: "${hyperfoil.phase.start.time.as.string}"
              ce-runid: "${hyperfoil.run.id}"
              ce-source: "http://hyperfoil-bench.com"
              ce-specversion: "1.0"
              ce-type: datapoint.hyperfoilbench
              content-type: application/json
            sla:
              - meanResponseTime: 1s
              - limits:
                  "0.999": 1s


    - broker-ord-9:
        - timestamp:
            toVar: eventTimestamp
        - httpRequest:
            POST: /${TEST_CASE_NAMESPACE}/broker-ord-9
            body: |
              {"foo" : "bar"}
            headers:
              ce-benchmarktimestamp: "${eventTimestamp}"
              ce-id: abc
              ce-metric: e2elatency
              ce-phase: "${hyperfoil.phase.id}"
              ce-phasestart: "${hyperfoil.phase.start.time.as.string}"
              ce-runid: "${hyperfoil.run.id}"
              ce-source: "http://hyperfoil-bench.com"
              ce-specversion: "1.0"
              ce-type: datapoint.hyperfoilbench
              content-type: application/json
            sla:
              - meanResponseTime: 1s
              - limits:
                  "0.999": 1s


